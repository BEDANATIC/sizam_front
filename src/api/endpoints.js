const COURSES = 'http://127.0.0.1:8000/courses/';
const SET_FINISHED = COURSES + 'set_finished';
const SET_APPROVED = COURSES + 'set_approved';
const SET_ACCEPTED = COURSES + 'set_accepted';
const SET_EXPELLED = COURSES + 'set_expelled';

export {
    COURSES,
    SET_FINISHED,
    SET_APPROVED,
    SET_ACCEPTED,
    SET_EXPELLED
};